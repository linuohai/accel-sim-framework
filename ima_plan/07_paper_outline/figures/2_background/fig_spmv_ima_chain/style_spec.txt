Generate a publication-quality academic diagram in a 3:4 vertical aspect ratio (single-column format) using a strict flat, 2D vector style with no shadows, no gradients, and no 3D effects. The background is pure white. All text is in English with dark #2C3E50 color unless specified. Use a 0-100 coordinate system for placement (X: 0=left, 100=right; Y: 0=top, 100=bottom). At top center (X:15 to 85, Y:5 to 20), draw a box with Light Gray #ECF0F1 fill, 1pt solid black border, and 4pt rounded corners. Inside, write the monospace code: `for (j = row_off[i]; j < row_off[i+1]; j++)` on the first line, and `y[i] += A_val[j] * x[ A_col[j] ];` on the second line. Above the box at top left, place the label "SpMV inner loop" in 9pt sans-serif bold. At Y:32, draw a horizontal array of 10 contiguous rectangular cells (spanning X:22 to 82). Each cell has a Light Blue #AED6F1 fill and 1pt black border. Inside the cells, write the numbers sequentially from left to right: 1, 4, 7, 2, 5, 9, 0, 6, 3, 8 in 9pt monospace bold. To the left of the array (X:5), add the main label "A_col[]" and sub-label "column indices (stride-1)" in 9pt sans-serif bold. Below the array (Y:42), draw a Green #27AE60 horizontal arrow pointing right, labeled "j →". Above the array, add the annotation "① index load: A_col[j]" in Red #C0392B. At Y:77 (leaving a 32-unit vertical gap), draw another horizontal array of 10 contiguous rectangular cells (spanning X:22 to 82) aligned exactly under the first array. Label the cells from left to right: x0, x1, x2, x3, x4, x5, x6, x7, x8, x9 in 9pt monospace bold. Color the cells at indices 1, 4, 7, 2, and 5 with Dark Purple #A569BD fill and pure WHITE text. Color the remaining cells (0, 3, 6, 8, 9) with Light Purple #D7BDE2 fill and dark #2C3E50 text. To the left (X:5), add the label "x[]" and sub-label "dense input vector (scattered)". Below the array, add the annotation "② data load: x[A_col[j]]" in Red #C0392B. Draw 5 distinct, non-tangling curved arrows in Red #C0392B (1.5pt solid) with small triangular arrowheads pointing from the bottom of the first 5 A_col[] cells to the top of the corresponding x[] cells. Arrow 1: from '1' to 'x1' (slight S-curve). Arrow 2: from '4' to 'x4' (downward arch). Arrow 3: from '7' to 'x7' (wide downward arch). Arrow 4: from '2' to 'x2' (leftward arch crossing under arrows 1 and 2). Arrow 5: from '5' to 'x5' (direct S-curve). Arrowheads must touch the top border of the x[] cells without overlapping the text. On the right side (X:84), draw a large thin black curly bracket spanning vertically from Y:32 to Y:90. Next to it (X:88), add the text "IMA chain: serial dependency" rotated 90 degrees clockwise. At the bottom (Y:94 to 98), draw a horizontal legend with four evenly spaced items: a Green #27AE60 arrow labeled "stride-1", a Red #C0392B arrow labeled "scattered", a Dark Purple #A569BD square labeled "accessed", and a Light Purple #D7BDE2 square labeled "not accessed".