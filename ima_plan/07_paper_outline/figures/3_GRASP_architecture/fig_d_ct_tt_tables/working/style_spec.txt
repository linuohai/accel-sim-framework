Create a professional academic diagram for a computer architecture paper (IEEE double-column full width, 16:9 aspect ratio). Use a pure white background with no shadows, gradients, or 3D effects. All standard text and labels must be in Times New Roman (minimum 8 pt), and all code, hex addresses, and register names must be in Monospace (Courier). All borders and outlines are black, 1–1.5 pt thickness. Do not render any layout metadata, section headers, or block labels.

In the top-left (spanning X: 2% to 50%, Y: 2% to 30%), draw the SpMV CUDA Source block as a light gray (#F5F5F5) rounded box. Add a small gray italic label "SpMV CUDA Kernel". The Monospace code is: line 1 "// CSR SpMV (base.cu:22)" (gray); line 2 "for (int offset = row_begin;" (gray); line 3 "     offset < row_end; offset++) {" (gray); line 4 "  sum += Ax[offset] * x[Aj[offset]];" (black bold); line 5 "}" (gray). On line 4, highlight "Aj[offset]" in blue #2B5EA7 with a small label "Index Load" below it, and highlight "x[...]" in amber #D4A843 with a small label "Data Load" below it. A gray dashed arrow (#808080) labeled "compiles to" in gray italic points rightward from this block to the SASS block.

Below the CUDA block (spanning X: 2% to 50%, Y: 35% to 60%), draw the Target Table (TT). It has a left-aligned bold title "Target Table (TT)" and a gray italic subtitle "8 entries". Do not add any address prefix labels near this table. The header row has a PURPLE (#6C3483) background with white bold text, containing columns: Valid, Base Address, and Scale. The table has three data rows plus an ellipsis row, and should be drawn taller/longer. Row 1: a solid GREEN (#5AA469) filled square, "0x7FC4B7C20000 (x[])", and "4" (with a small gray annotation "sizeof(float)" near the 4). Row 2: solid GREEN square, "0x7FC4B7E40000 (y[])", and "4". Row 3: solid GREEN square, "0x7FC4B8100000 (z[])", and "8". Row 4 (ellipsis): "...", "...", "..." in gray italic to indicate 5 more entries. Base addresses must be in monospace and written in FULL (all digits, no abbreviation). Array names in italic. The Base Address column should be wide enough to fit the full 14-digit hex addresses.

On the right side (spanning X: 55% to 98%, Y: 2% to 60%), draw the SASS Assembly block as a light gray (#F5F5F5) rounded box with a gray italic label "SASS Assembly (×16 Unrolled Loop)". The Monospace code shows 14 lines. Lines 1 and 2 are "/*0x03d0*/ LDG.E R9, [R6+-0x8]" and "/*0x03e0*/ LDG.E R27, [R6+-0x4]" in BLUE (#2B5EA7) bold. Lines 3-5 are "/*0x03f0*/ LDG.E R13, [R6]", "/*0x0400*/ LDG.E R21, [R6+0x4]", and "... (12 more index loads)" in gray (line 5 italic). Lines 6 and 7 are "/*0x0480*/ IMAD.WIDE R8, R9, R16, c[0x180]" and "/*0x0490*/ IMAD.WIDE R26, R27, R16, c[0x180]" in PURPLE (#6C3483) bold. Lines 8-9 are "/*0x04b0*/ IMAD.WIDE R12, R13, R16, c[0x180]" and "... (13 more IMAD.WIDEs)" in gray (line 9 italic). Lines 10 and 11 are "/*0x04a0*/ LDG.E R14, [R8]" and "/*0x04c0*/ LDG.E R24, [R26]" in AMBER (#D4A843) bold. Lines 12-14 are "/*0x04d0*/ LDG.E R12, [R12]", "... (13 more data loads)", "/*0x0580*/ FFMA R14, R14, R18, R11", and "... (accumulation)" in gray (italic for ellipses). Add right-side brackets grouping the phases: Lines 1-5 labeled "IMA Index Load" (blue #2B5EA7), Lines 6-9 labeled "Addr Generation" (purple #6C3483), and Lines 10-13 labeled "IMA Data Load" (amber #D4A843).

Centered horizontally below the top region (X: 25% to 75%, Y: 62% to 68%), draw a formula bar with a light background (#EBF5FB) and a 0.5 pt border. The LaTeX-style Times New Roman text is "Addr_{target} = base + scale × index" (using proper subscript for target). Color "base" and "index" in blue #2B5EA7, and "scale" in purple #6C3483. Draw short arrows pointing UP from the blue "base" text to the TT "Base Address" column, and from the purple "scale" text to the TT "Scale" column. 

In the bottom-left/center (X: 2% to 70%, Y: 72% to 98%), draw the Chain Table (CT). It has a left-aligned bold title "Chain Table (CT)" and a gray italic subtitle "32 entries". Do not add any address prefix labels near this table. The header uses a two-layer DMP-style layout with a BLUE (#2B5EA7) background and white bold text, with 2px white gaps between three groups. Group 1 is "Identification" spanning sub-columns "Valid" and "Index PC". Group 2 is "Target Mapping" spanning "TT Index" (which itself spans sub-cells "[0]", "[1]", "[2]") and "K". Group 3 is "Stride Learning (IST)" spanning "Stride Obs" (spanning sub-cells "[0]", "[1]"), "Iter Stride", and "Stride Valid". The data rows have a PURE WHITE (#FFFFFF) background (NOT gray — tables must be visually distinct from the gray code blocks), black text, and 1 pt borders. Row 1: [solid GREEN square], 0x03d0, 0, —, —, 1, W5: 0x7FC4D0DA4864, W5: 0x7FC4D0DA48A4, 64 B, [solid GREEN square]. Row 2: [solid GREEN square], 0xA120, 0, 1, —, 2, W3: 0x7FC4D0DA2100, W3: 0x7FC4D0DA2110, 16 B, [solid GREEN square]. Row 3: [solid GREEN square], 0xB340, 0, 1, 2, 3, W8: 0x7FC4D0DA5040, —, 4 B, [empty cell]. All hex addresses in Stride Obs must be written in FULL (all digits, no abbreviation with "0x..."). Row 4: ..., ..., (empty), (empty), (empty), ..., (empty), (empty), ..., ... (use ellipses to indicate continuation).

In the bottom-right (X: 75% to 98%, Y: 72% to 98%), draw the IST block as a light blue (#E8EEF8) rounded box with a 1 pt blue (#2B5EA7) outline. Do NOT use gray (#F5F5F5) for IST — it must be visually distinct from the gray code blocks (CUDA and SASS). It has a bold title "IST" and a small gray subtitle "Iteration Stride Tracker". The text inside is: "A₁ = 0x7FC4D0DA4864 @ iter i", "A₂ = 0x7FC4D0DA48A4 @ iter i+1", "stride = A₂ − A₁ = 0x40 = 64 B ✓", and "★ Any warp can contribute". A solid black arrow points LEFT from the IST block to the CT "Iter Stride" / "Stride Valid" columns.

Add the following routing arrows: Draw solid black arrows from SASS lines 1 and 2 routing DOWNWARD, bypassing the formula, connecting to CT Row 1 and Row 2 "Index PC" cells, labeled "①" and "②". Draw solid black arrows from the CT "TT Index [0]" column routing UPWARD-LEFT, converging into a single arrow pointing to the TT. Draw gray dashed arrows from "c[0x180]" in the SASS IMAD.WIDE instructions routing LEFT to the TT "Base Address" column, and from "R16" routing LEFT to the TT "Scale" column. Do NOT add any gray annotation boxes or text annotations — the arrows and table content are self-explanatory.
