Generate a publication-quality academic diagram in 4:3 aspect ratio designed for a computer architecture paper targeting MICRO/ISCA/HPCA conferences. The canvas must be exactly 800 pixels wide by 600 pixels high, featuring a pure white background with hex code #FFFFFF. There must be absolutely no title text anywhere on the canvas, no Chinese characters, no shadows, no gradients, and no 3D effects. The visual style must be a clean, neutral, high-contrast academic vector rendering. The layout is divided into two vertically stacked sections: Panel (a) occupies the top 42% of the canvas, and Panel (b) occupies the bottom 55%, with a 3% vertical whitespace gap between them and 5% padding on the left and right margins. All SASS instructions, register names, and hex values must be rendered in a clear monospace font, while all labels, annotations, and callouts must use a clean sans-serif font. 

Begin with Panel (a) at the top. In the top left at approximately 5% width and 4% height, place the panel label "(a) SASS Chain for x[A_col[j]]" in 10pt bold dark slate gray (#2C3E50) sans-serif font. Below this label, center three instruction boxes vertically. At 25% width and 10% height, spanning 50% width and 8% height, draw the first box as a rounded rectangle with a light gray (#ECF0F1) background, a 1pt solid black border, and 4pt rounded corners. Inside this box, horizontally and vertically centered, write exactly "LDG.E.SYS  R10, [R6]" in 10pt dark monospace font. To the immediate left of this box at 20% width, draw a numbered step circle consisting of a 16pt dark circle containing a bold white number "1". To the immediate right of the box at 77% width, write "index load" in 9pt italic dark gray sans-serif font. Below this, at 24% height, draw an identical second rounded rectangle. Inside, write exactly "IMAD.WIDE  R12, R10, 0x8, c[0x0][0x178]" in 10pt dark monospace font. To the left at 20% width, draw a dark circle with a bold white "2". To the right at 77% width, write "address compute" in 9pt italic dark gray. Below this, at 38% height, draw a third identical rounded rectangle. Inside, write exactly "LDG.E.SYS  R14, [R12]" in 10pt dark monospace font. To the left, draw a dark circle with a bold white "3". To the right, write "data load" in 9pt italic dark gray. Connect these three boxes with solid red (#C0392B) arrows, 1.5pt thick, with small triangular heads to show register dependency flow. Draw the first arrow from the bottom-center of the first box to the top-center of the second box. To the right of this arrow, write exactly "R10 carries loaded index" in 8pt italic red (#C0392B) sans-serif font. Draw the second arrow from the bottom-center of the second box to the top-center of the third box. To the right of this arrow, write exactly "R12 carries computed address" in 8pt italic red (#C0392B) sans-serif font.

Move to Panel (b) in the lower section. At 50% height on the left margin, place the label "(b) IMAD.WIDE Operand Decomposition" in 10pt bold dark (#2C3E50) sans-serif font. This panel is the core visual and features a horizontal breakdown of the IMAD.WIDE instruction. At 60% height, horizontally centered, display the instruction. Start with the opcode "IMAD.WIDE" at 10% width in 11pt bold dark monospace font. Following this, draw four distinct colored rounded boxes, each with a 2pt corner radius and a 1pt black border, separated by dark 10pt regular monospace commas. At 28% width, draw the first box filled with purple (#7B68AE). Inside, write exactly "R12" in 10pt bold white monospace font, followed by a comma outside the box. At 40% width, draw the second box filled with red (#C0392B). Inside, write exactly "R10" in 10pt bold white monospace font, followed by a comma. This red must exactly match the red of the arrows in Panel (a) to reinforce the data dependency flow. At 52% width, draw the third box filled with amber yellow (#D4A843). Inside, write exactly "0x8" in 10pt bold white monospace font, followed by a comma. At 64% width, draw a wider fourth box filled with green (#5AA469). Inside, write exactly "c[0x0][0x178]" in 10pt bold white monospace font. 

From the bottom center of each of these four colored operand boxes, drop a thin, straight, vertical gray line (#95A5A6, 0.8pt thick) down to an explanation block below. These lines drop from approximately 64% height to 72% height. Arrange four explanation blocks horizontally below the lines, starting at 74% height. Each block contains a 3x3 pixel square bullet matching its operand's color, followed by left-aligned 8pt dark (#2C3E50) sans-serif text. For the first block under R12, use a purple square bullet (#7B68AE) and write "output" on the first line, and "address of x[A_col[j]]" on the second line. For the second block under R10, use a red square bullet (#C0392B) and write "index value" on the first line, and "from ① at runtime" on the second line. For the third block under 0x8, use an amber yellow square bullet (#D4A843) and write "scale factor" on the first line, "sizeof(double)" on the second line, and "compile-time immediate" on the third line. For the fourth block under c[0x0][0x178], use a green square bullet (#5AA469) and write "base pointer of x[]" on the first line, "kernel parameter" on the second line, and "constant memory" on the third line. Ensure the color coding in Panel (b) is the dominant visual feature, separating the four operands distinctly to highlight the zero-inference insight. The entire figure must be fully self-contained, perfectly legible at a 3.5-inch print width, and strictly adhere to the exact spelling, colors, and spatial relationships described.